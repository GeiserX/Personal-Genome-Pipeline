/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Use: nextflow run main.nf -profile test,docker
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'

    // Limit resources for CI
    max_cpus   = 2
    max_memory = '4.GB'
    max_time   = '1.h'

    // Test data — uses stub inputs for CI
    // For real testing, override with actual paths:
    //   nextflow run main.nf -profile test,docker \
    //     --input samplesheet.csv \
    //     --reference /path/to/GRCh38.fasta
    input     = "${projectDir}/assets/test_samplesheet.csv"
    outdir    = './results_test'
}
